[package]
name = "rtos-shell"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Hosted playground shell for rustytempleOS (Phase 1 — not freestanding kernel yet)"

[[bin]]
name = "rtos-shell"
path = "src/main.rs"
